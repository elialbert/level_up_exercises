.page-header
  %h1 Favorites

%table
  %thead
    %tr
      %th Menu Item Name
      %th Location
  %tbody
    - current_user.favorites.each do |favorite|
      %tr
        %td= favorite.menu_item.name
        %td= temp
